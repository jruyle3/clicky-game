import React from "react";

const Header = () => (
  <div className="jumbotron p-3">
    <h1 className="text-center">Click LeBron</h1>
    <h3 className="text-center">Click on an image to earn points, but don't click on the same image twice!</h3>
  </div>

)
export default Header;